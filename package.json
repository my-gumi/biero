{
  "name": "biero",
  "version": "0.1.0",
  "description": "Behavioral Intelligence for Evaluating Risk & Optimization — a local-first stock AI assistant CLI.",
  "type": "module",
  "bin": {
    "biero": "dist/bin/biero.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "watch": "tsc --watch",
    "start": "node dist/bin/biero.js",
    "setup": "node dist/bin/biero.js setup",
    "test": "node --test tests/*.mjs",
    "prepare": "tsc"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "discord.js": "^14.26.4",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
